name: Bootstrap Rust MSYS2
on:
  schedule:
    - cron: '43 10 * * *'
  workflow_dispatch:

jobs:
  cross_rust:
    runs-on: windows-latest

    steps:
      - run: git config --global core.autocrlf input
      - uses: actions/checkout@v5
      - id: msys2
        uses: msys2/setup-msys2@v2
        with:
          release: true
          update: true
          msystem: MSYS
          install: >-
            base-devel
            gcc
            llvm
            python
            libgit2-devel
            libssh2-devel
            openssl-devel
            pcre2-devel
            pkgconf
            libsqlite-devel
            liblzma-devel
            libzstd-devel
            zlib-devel
            libcurl-devel
            mingw-w64-x86_64-python
            mingw-w64-x86_64-llvm
            mingw-w64-x86_64-clang
      - name: build
        run: |
          mkdir bin
          gcc -o bin/llvm-config.exe llvm-config-wrapper.c
          gcc -o bin/gcc.exe wrapper.c
          for b in g++ ar ranlib x86_64-pc-cygwin-gcc curl-config; do
            ln bin/gcc.exe bin/${b}.exe
          done
          #RUST_URL="$(python3 -c "import tomli; from urllib.request import urlopen; x = tomli.load(urlopen('https://static.rust-lang.org/dist/channel-rust-nightly.toml')); a = [y for y in x['artifacts']['source-code']['target']['*'] if y['url'].endswith('.xz')]; print(a[0]['url'])")"
          RUST_URL="https://static.rust-lang.org/dist/rustc-1.89.0-src.tar.xz"
          echo "$RUST_URL" | tee rust-url.txt
          curl -LO "$RUST_URL"
          MSYS=winsymlinks:sys tar -Jxf rustc-1.89.0-src.tar.xz
          cd rustc-1.89.0-src
          MSYS2_ROOT="$(cygpath -m /)" WORKSPACE="$(cygpath -ma "../bin")" envsubst < ../bootstrap.toml > bootstrap.toml
          export PATH="$(cygpath -ua ../bin):/mingw64/bin:$PATH"
          unset PKG_CONFIG_PATH
          PKG_CONFIG_SYSROOT_DIR="$(cygpath -w /)" \
          PKG_CONFIG="$(cygpath -w /usr/bin/pkgconf.exe)" \
          LIBGIT2_NO_VENDOR=1 \
          OPENSSL_DIR="$(cygpath -w /usr)" \
          OPENSSL_LIB_DIR="$(cygpath -w /usr/lib)" \
          OPENSSL_STATIC=0 \
          LIBSQLITE3_SYS_USE_PKG_CONFIG=1 \
          DESTDIR="$(cygpath -w "$PWD/build-Cygwin/dest-rust")" \
          python x.py dist
        shell: msys2 '{0}'
      - uses: actions/upload-artifact@v4
        with:
          name: rust-prefix
          path: |
            rustc-1.89.0-src/build-Cygwin/dist/*
